# Example environment variables for Nasa-Back-main
# Copy to .env and fill values (do NOT commit your real keys)
GEMINI_API_KEY=
# Optional: change model if you have access to another one
GEMINI_MODEL=gemini-2.5-pro
FLASK_ENV=development
FLASK_APP=run.py
